# _inbox — capturas TUI sin libro asignado

Fotos que llegan al chat Grok sin `--book`. El script `scripts/ingest-tui-img.sh` deja aquí lo desconocido.

Mover a `Citas <Libro>/` o a `ciper-mvp/assets/inbox-clave-a/` en cuanto se identifique el soporte. No es editorial.
